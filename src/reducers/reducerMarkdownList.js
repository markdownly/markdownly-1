/* eslint-disable no-unused-vars */
const initialState = {
  markdowns: [{
    title: 'title',
    markdown: 'text'
  }]
};

export default function reducer(state = initialState, action) {
  return state;
}
